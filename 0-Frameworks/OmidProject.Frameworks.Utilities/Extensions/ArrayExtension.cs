namespace OmidProject.Frameworks.Utilities.Extensions;

public static class ArrayExtension
{
    // مرتب سازی آرایه ها
    public static Array Sort(this Array input)
    {
        // مرتب سازی آرایه ها
        Array.Sort(input);

        return input;
    }
}